services:
  db:
    restart: always

  redis:
    image: redis:7-alpine
    restart: always

  backend:
    ports:
      - "80:8080"
    restart: always

  frontend:
    ports:
      - "3000:3000"
    restart: always
    environment:
      - NEXT_PUBLIC_API_URL=http://your-production-domain.com
